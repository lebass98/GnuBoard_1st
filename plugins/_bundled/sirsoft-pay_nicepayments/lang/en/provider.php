<?php

/**
 * NicePayments PG provider meta (English)
 *
 * Label referenced by name_key in the
 * `sirsoft-ecommerce.payment.registered_pg_providers` hook payload.
 * Resolved via the active language pack at runtime.
 */
return [
    'name' => 'NicePayments',
    'description' => 'NicePayments (Korean PG) — card / bank transfer / virtual account / mobile + 7 easy-pay providers',
];
